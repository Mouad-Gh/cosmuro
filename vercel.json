{
    "version": 2,
    "builds": [
        {
            "src": "./backend/server.js",
            "use": "@versel/node"
        }

    ],
    "routes": [
        {
            "src": "./backend/(.*)",
            "dest": "/backend/"
        }
    ]
}